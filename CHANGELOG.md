# 📝 版本发行说明

> 最新版本见顶部，历史版本与完整更新日志见后文。

---

## 🔴 塔科夫血量监控 v2.1.0 (2026-09-03)

---

### ✨ 新增功能

- **版本定档 v2.1.0**：以 v1.2.0 全新寻址内核代码为基线，将 `manifest.json` / README / 成品包版本号统一为 `2.1.0`，修正 DGHub 侧残留的 2.0.0"版本倒挂"（旧代码但版本号更高，导致部署被误判为降级）
- **新增本发行说明文件**（`CHANGELOG.md`）：集中记录各版本新增功能、配置项、改进与修复

---

### 🔧 改进

- **README 主页介绍全面更新**：突出"免 CE 自动寻址 + Mod 双数据源"；新增配套 SPT Mod 安装小节，介绍反射读取、跨版本容错与 HTTP 契约
- **发行文档 Mod 命名订正**：`StateMod`（规划名）→ `TarkovCoyoteSptHealthMod`（实际落地工程名），与新 Mod 工程保持一致
- **成品包结构规范化**：
  ```
  成品/
  ├─ tarkov_health_monitor_v2.1.0.zip      DGHub 外部插件
  ├─ SPT_Mod_TarkovCoyoteSptHealthMod\
  │   └─ TarkovCoyoteSptHealthMod.dll      SPT Mod（BepInEx）
  └─ 安装说明.txt
  ```

---

### 🐛 修复

- **多文件版本号不一致**：manifest / README 徽章 / 概述 / 成品包名 / 安装说明统一到 `2.1.0`

---

### 📦 安装

1. 下载 `tarkov_health_monitor_v2.1.0.zip`
2. DGHub → 插件中心 → 外部插件 → 导入 zip
3. 启用插件 → 配置参数 → 开始监控

> 💡 可选：将 `TarkovCoyoteSptHealthMod.dll` 放入 `BepInEx/plugins/`，插件优先走 Mod 数据通道（更稳定），Mod 不可用时自动回退内存读取。

---

## 🔴 塔科夫血量监控 v1.2.0 (2026-09-03)

---

### ✨ 新增功能

- **全新自动扫描内核 `AutoScanEngine`**（`memory.py` 重构），五层策略大幅提升精度与速度：
  1. **区域化扫描**：`VirtualQueryEx` 只遍历「已提交 + 可读可写」内存区域，跳过代码段/只读资源，速度更快、噪声骤减
  2. **4 字节对齐 + 纯 float 初筛**：只收落在 1~500 的有限 float（血量必然是 float32），int/指针/ASCII 噪声从源头排除
  3. **稳定性过滤**：间隔二次读取，剔除瞬时抖动/越界的伪候选
  4. **期望值匹配**：已知当前血量（Mod 通道正常）时可精确收敛
  5. **多轮掉血差分收敛**：等待受伤事件，只保留"同步掉血"的地址；血量镜像一起掉血，经 1~3 次受伤收窄到唯一地址；静态高值与体力等不随掉血变化的 float 自动剔除
- **配套 SPT Mod 工程落地**（`Tarkov-Coyote-SPT-Mod`，net48 / BepInEx）：补全 v1.1.0 规划的 Mod 数据源（此前 `mod_client.py` 契约无对应实现）
  - `GameStateReader` 反射读取血量，候选名列表跨版本容错（游戏大版本改名只需追加字段）
  - 本地 HTTP `127.0.0.1:8765` 暴露 `/playerinfo`、`/status`
  - 主菜单 / 死亡时省略 `health.total`，插件自动回退内存读取，不会用 0 值误触发

---

### 🔧 改进

- **`plugin.py` 接入状态机编排**：`AutoScanEngine` 以 `start / poll / result / drain_logs` 驱动，executor 执行不阻塞消息与心跳；日志/状态实时上报 DGHub
- **差分轮询节奏**：`wait_damage` 阶段每 5 秒自动复查（`AUTO_POLL_DELAY`），受伤后自动收窄；失败按 10 秒冷却重建重扫（`SCAN_RETRY_DELAY`），不会热循环刷屏
- **状态机生命周期管理**：配置变更、游戏重连、地址失效时自动重置扫描器，杜绝跨会话沿用旧候选
- **DGHub 界面可视化**：扫描过程中实时显示「候选 N → 等待受伤差分确认」，收敛后自动回填地址恢复监控
- **README**：新增「方式 C：自动扫描（默认，免 CE）」使用要点与 FAQ 说明

---

### 🐛 修复

- **候选残留导致数量虚高**：`poll` 时读取失效/越界的候选不再残留在候选中（现同步清理）
- **阵亡 / 离图误判**：数值归零时正确判为失败并等待重扫，不再用失效地址持续误读
- **扫描噪声与崩溃风险**：不再遍历只读/代码段区域
- **net48 兼容**：Mod 工程计时 API 修正（`Environment.TickCount64` → `DateTime.UtcNow.Ticks`，该 API 在 .NET 5+ 才存在）

---

### 📦 安装

1. 下载 `tarkov_health_monitor_v1.2.0.zip`
2. DGHub → 插件中心 → 外部插件 → 导入 zip
3. 启用插件 → 配置参数 → 开始监控

> 💡 寻址模式默认 `auto_scan`；请在**游戏内已出生、血量 1~500** 时启用，提示"等待受伤差分确认"后主动受一次伤（勿立刻治疗），插件会在数秒内自动复查收窄。

---
---

## 🔴 塔科夫血量监控 v1.1.0

---

### ✨ 新增功能

- **模块化架构重构**：将插件拆分为 7 个独立模块，提高可维护性和扩展性
- **SPT Mod 数据源支持**：可通过 `use_mod` 配置项启用，优先从 SPT Mod 获取血量数据（更稳定），失败时自动回退到内存读取
- **Mod 数据源开关**：新增 `use_mod` 配置开关，支持在 DGHub 配置页面实时开启/关闭 Mod 数据源，无需重启插件
- **精简版 StateMod 兼容**：提供配套的 SPT Mod（StateMod 2.0.2），只读取总血量，完全兼容 Fika 联机模式

---

### 📊 新增配置项

| 配置项 | 说明 | 默认值 |
|--------|------|--------|
| `use_mod` | 优先使用 Mod 数据源 | `true` |
| `mod_port` | SPT Mod HTTP 端口 | `8765` |

---

### 🔧 改进

- **代码模块化**：将原来的单文件架构拆分为 7 个独立模块，职责清晰
- **Mod 通信优化**：支持配置热更新，开关 Mod 数据源无需重启插件
- **内存读取优化**：保留三种寻址模式（手动/AOB/全自动扫描），支持动态伤害强度
- **配置热更新增强**：所有配置项（包括 Mod 开关）修改后立即生效

---

### 🐛 修复

- **Mod 开关无效问题**：修复了 `use_mod` 配置开关无法控制功能开启/关闭的问题
- **与 Fika 联机冲突**：精简版 StateMod 只读总血量，避免与 Fika 联机 Mod 冲突导致崩溃
- **配置变更回调**：修复切换 Mod 数据源时状态未正确重置的问题

---

### 📦 安装

1. 下载 `tarkov_health_monitor_v1.1.0.zip`
2. DGHub → 插件中心 → 外部插件 → 导入 zip
3. 启用插件 → 配置参数 → 开始监控

> 💡 如需使用 Mod 数据源，请安装配套的 `SPT_StateMod.dll`（放入 `BepInEx/plugins/`）

---

### 📜 完整更新日志
v1.1.0 (2025-08-27)
├── ✨ 模块化架构重构（7 个独立模块）
├── ✨ 新增 SPT Mod 数据源支持
├── ✨ 新增 use_mod 配置开关
├── 📊 新增 2 个配置项
├── 🔧 优化 Mod 通信与热更新
├── 🐛 修复 Mod 开关无效问题
├── 🐛 修复与 Fika 联机冲突
└── 📝 更新 README 和发行说明

v1.0.9 (2025-08-27)
├── 🐛 修复手动模式误扫描
├── 🐛 修复配置持久化
├── 🐛 修复地址验证与异步阻塞
├── 🐛 修复状态残留与 AOB 解析
├── 🔧 统一版本号
└── 📝 更新 README 依赖说明

v1.0.8 (2025-08-26)
├── ✨ 新增全自动扫描模式
├── 📊 新增 3 个配置项
├── 📝 优化内存扫描算法
└── 🔄 支持三种寻址模式切换

v1.0.7 (2025-08-26)
├── ✨ 新增 AOB 特征码扫描
└── 📊 新增 2 个配置项

v1.0.6 (2025-08-26)
├── 🔄 版本号升级
├── ✨ 新增动态伤害强度
├── 🇨🇳 全面中文化
└── 🔄 支持配置热更新

v1.0.0 (初始版本)
├── 🎉 支持基址+偏移链读取血量
└── ⚡ 集成 DGHub trigger 反馈


---

### 🙏 致谢

- [hyperzlib](https://github.com/hyperzlib) – DGHub 开发者
- [DG-LAB](https://www.dg-lab.com/) – 郊狼设备
- SPT 社区所有测试者

---

**完整文档**：[GitHub README](https://github.com/qtjtrbxqw-ux/Tarkov-Coyote-Plugin)
